<div class="docs-container">
  <div class="controls-block">
    <%= form_tag(documentation_index_path, method: :get, class: "form-inline") do %>
      <div class="form-group">
        <%= text_field_tag :search,
          params[:search],
          class: "form-control",
          placeholder: "Search across all fields..."
        %>
        <%= submit_tag "Search", class: "btn btn-primary control-btn" %>
      </div>
    <% end %>
    <div>
      <%= link_to "Download CSV",
        documentation_index_path(format: "csv"),
        class: "btn btn-secondary control-btn"
      %>
    </div>
  </div>
  <!-- Grid Table Container -->
  <div class="grid-table">
    <!-- Grid Header -->
    <div class="grid-row grid-header">
      <div>Active</div>
      <div>Table</div>
      <div>Column</div>
      <div>Type</div>
      <div>Description</div>
      <div>CTGov Field</div>
      <div>Details</div>
    </div>
    <!-- Grid Rows -->
    <% @paginated_docs.each do |doc| %>
      <div class="grid-row">
        <div><%= doc["active"] ? "Yes" : "No" %></div>
        <div><%= doc["table_name"] %></div>
        <div><%= doc["column_name"] %></div>
        <div><%= doc["data_type"] %></div>
        <div><%= doc["description"] || "-" %></div>
        <div><%= doc["ctgov_data_point_label"] || "-" %></div>
        <div>
          <button class="details-button">Details</button>
        </div>
      </div>
    <% end %>
  </div>
  <%= paginate @paginated_docs %>
</div>