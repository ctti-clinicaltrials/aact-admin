<section class="learnMore">

  <p>All ClinicalTrials.gov data are available in the <i>ctgov</i> schema of the AACT database. The <i>ctgov</i> schema also includes two tables from another source; <i>mesh_terms</i> & <i>mesh_headings</i> provide a recent copy of the <a href='https://www.ncbi.nlm.nih.gov/mesh' target='_blank'>Medical Subject Headings (MeSH terms & headings)</a> published by the National Library of Medicine. These 2 tables are included to help people who use MeSH terms to categorize studies. (ClinicalTrials.gov uses <i>browse_conditions</i> & <i>browse_interventions</i> to link studies to related MeSH terms.)</p>

  <p>To facilitate public sharing of information acquired during previous research efforts, the AACT database also includes 'project schemas'. These schemas allow any database user to access select sets of derived data artifacts that were gathered & curated during previous AACT-based research. Currently, data for three projects are available in AACT. Click <a href='/projects/' target="_blank">here</a> to learn more about these projects.</p>

  <p>The schema diagram below shows the current set of schemas and tables that comprise this 'AACT Projects' feature.</p>

  <section class="schema">
     <%= image_tag(@proj_schema_diagram) %>
  </section>


  <p>The National Library of Medicine updates the MeSH Thesaurus each year. Select annual versions of MeSH terms are provided in the <i>mesh_archive</i> schema.</p>
  <p>The database also includes a <i>support</i> schema which contains only tables needed to update & maintain the information in the ctgov schema. A companion database also exists, AACT_ADMIN, which supports this website and user registration.</p>
  <p>AACT schema diagrams and documentation may be downloaded from the following links:</p>
  <ul class='regularDisplay'>
        <li><a href=<%= @schema_diagram %> target="_blank" onClick="ga('send','event','download','download schema')"  >downloadable schema diagram</a> (right click on the resulting page to download)</li>

        <% if @show_dictionary_link %>
          <li><a href=<%= @data_dictionary %> target="_blank" onClick="ga('send','event','download','download data dictionary')">excel version of data dictionary</a> (online searchable version also available <a href='/data_dictionary' target='_blank'>here</a>)</li>
        <% else %>
          <li><a href=<%= @data_dictionary %> target="_blank" onClick="ga('send','event','download','download data dictionary')">excel version of data dictionary</a></li>
        <% end %>
        <li><a href=<%= @table_dictionary %> target="_blank" onClick="ga('send','event','download','download data dictionary')">excel version of table definitions</a></li>

  </ul>

    <p>Please refer to the National Library of Medicine's (NLM) documentation for authoritative definitions of the data elements:</p>
    <ul class='regularDisplay'>
      <li><a href="https://prsinfo.clinicaltrials.gov/definitions.html" target="_blank" onClick="ga('send','event','external link','schema page: NLM Protocols')">registered protocol</a></li>
      <li><a href="https://prsinfo.clinicaltrials.gov/results_definitions.html" target="_blank" onClick="ga('send','event','external link','schema page: NLM Results')">study results</a></li>
    </ul>
    <% if @show_dictionary_link %>
      <p>The <a href='/data_dictionary' target='_blank'>online data dictionary</a> includes links to sections of this NLM documentation to provide documentation about specific data element.</p>
    <% end %>

</section>

