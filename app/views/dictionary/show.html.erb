<section class="dataDictionaryShow">
  <main>
    <h1>AACT Data Dictionary</h1>

    <section>
      <p>AACT is composed of <%= @tables.size %> tables that provide information related to clinical trials. Every table is related by NCT ID to the Studies table. Click <a href='#tableDictionary'>here</a> to view a listing of all <%= @tables.size %> tables with description and row counts. Detailed information about all data elements included in these tables can be found in the data dictionary below.</p>
      <%= render 'pages/download_database_specs' %>
    </section>

    <h2>AACT Data Elements</h2>
    <%= render 'data_dictionary_instructions' %>
    <div class="gridContainer">
      <div id="jsGrid"></div>
    </div>
    <%= render 'table_dictionary' %>
    <%= render 'view_dictionary' %>
  <main>

<%= render "javascript" %>
</section>
